Config = {
    -- Grupos que pueden usar los comandos de admin
    AdminGroups = {'admin', 'superadmin', 'god'},

    -- Comandos
    Commands = {
        viewPeds = 'verpeds',
        playerPeds = 'peds'
    },

    -- Notificaciones
    Notifications = {
        noPermission = 'No tienes permiso para usar este comando.',
        pedGiven = 'Ped asignado correctamente.',
        playerNotFound = 'Jugador no encontrado.',
        noPeds = 'No tienes peds asignadas.'
    }
}
